import { Cormorant_Garamond } from "next/font/google"

/** Light italic serif for the AllurinaScarf wordmark (header / footer). */
export const brandLogoSerif = Cormorant_Garamond({
  subsets: ["latin", "latin-ext"],
  weight: "300",
  style: "italic",
  display: "swap",
})
